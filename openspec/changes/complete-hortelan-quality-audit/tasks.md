## 1. Baseline and SDD Reconciliation

- [x] 1.1 Capture a machine-readable baseline for Git hygiene, direct dependency currency, lint, formatting, tests, coverage, build contents, budgets, audit and strict OpenSpec validation
- [x] 1.2 Map every `modernize-hortelan-landing` task to implemented files, missing acceptance behavior and required evidence without changing its checkbox state prematurely
- [x] 1.3 Record the canonical and secondary hosting contracts, supported Node range, browser matrix and measurable release criteria in project documentation

## 2. Reproducible Repository and Current Toolchain

- [x] 2.1 Remove only tracked `node_modules` entries from the Git index, keep generated directories ignored and add an automated tracked-generated-file check
- [x] 2.2 Align CI and local engines on supported Node releases and add an early engine-contract validation test
- [x] 2.3 Align compatible direct packages, add React JSX reference analysis and document the intentionally deferred ESLint/jsdom majors
- [x] 2.4 Regenerate and verify the lockfile, then prove that a clean `npm ci` reconstructs the project without tracked package contents
- [x] 2.5 Run dependency-tree validation and low-through-critical vulnerability audit with zero unresolved production or high-severity development findings

## 3. Deterministic Static Analysis and Unit Coverage

- [x] 3.1 Repair all ESLint errors and warnings under the current flat configuration without disabling accessibility, hooks or injection protections
- [x] 3.2 Make shared test setup reset language, metadata, storage, theme, timers, mocks and browser API stubs between tests
- [x] 3.3 Fix locale-dependent application tests and cross-realm theme assertions while retaining the original behavioral expectations
- [x] 3.4 Add missing unit and component tests for invalid forms, storage write failures, export lifecycle, video activation, mobile-menu Escape/focus behavior, metadata updates and route/base normalization
- [x] 3.5 Expand coverage to behavior-bearing components and hooks, enforce global 90/90/85/85 thresholds and complete branch coverage for critical normalization, persistence and safe-content modules
- [x] 3.6 Run lint, format check, unit tests and coverage repeatedly to demonstrate deterministic success

## 4. Asset Graph and Complete Performance Budgets

- [x] 4.1 Replace the whole-directory copy plugin with one Vite-owned runtime asset graph and one stable public metadata asset set
- [x] 4.2 Consolidate the dashboard proof into a single emitted file and remove unreferenced oversized brand variants from the deployable artifact while preserving source artwork
- [x] 4.3 Update preload, favicon, manifest, social metadata and component references to the consolidated asset locations with correct intrinsic dimensions and alternative text
- [x] 4.4 Extend the budget analyzer to enumerate the complete artifact, classify initial references, detect duplicate or obsolete assets and enforce total, initial and per-media limits
- [x] 4.5 Add budget and artifact-verifier tests for nested files, unhashed public files, missing references, duplicate content, oversized media and threshold boundaries

## 5. Portable and Secure Production Build

- [x] 5.1 Introduce one tested base-path abstraction for HTML, application assets, manifest entries and legacy-path normalization without changing canonical production URLs
- [x] 5.2 Build and crawl root-hosted and repository-subpath variants, proving every emitted first-party HTML, CSS, manifest and build-manifest reference resolves
- [x] 5.3 Add canonical-host response configuration for CSP, Permissions-Policy, Referrer-Policy and anti-framing controls and remove unsupported or overbroad metadata directives
- [x] 5.4 Add automated static metadata and security-policy validation for JSON-LD, canonical/social URLs, icons, robots, sitemap and optional YouTube activation
- [x] 5.5 Document canonical-host enforcement and secondary GitHub Pages limitations with tested deploy and rollback commands

## 6. Real-Browser Functional and Accessibility Assurance

- [x] 6.1 Add Playwright projects for current Chromium, Firefox and WebKit with production-server startup, deterministic fixtures and failure on unexpected console, page, asset or request errors
- [x] 6.2 Automate the critical journey for internal navigation, all four locales, both themes, contact mailto validation, mural persistence/removal/export and markup-like inert content
- [x] 6.3 Add keyboard acceptance for skip link, mobile navigation, Escape/focus restoration, FAQ, preferences, invalid-field focus and live status announcements
- [x] 6.4 Add axe-core scans for the initial view, open navigation, expanded FAQ, invalid form and populated mural with zero serious or critical violations
- [x] 6.5 Verify 320, 375, 768, 1024, 1440 and 1920 CSS pixel layouts for horizontal overflow, clipping, overlaps, readable content, media integrity and primary target sizing
- [x] 6.6 Verify reduced-motion, forced-colors, light/dark contrast, storage denial/quota failures, deferred video loading and initial first-party-only network behavior

## 7. CI/CD and Immutable Promotion

- [x] 7.1 Make one local release command run strict OpenSpec, generated-file hygiene, lint, format, deterministic coverage, build, artifact integrity, complete budgets, dependency audit and browser acceptance
- [x] 7.2 Update CI to use the supported Node contract, run the authoritative release checks for code and OpenSpec Markdown changes and retain actionable coverage/browser artifacts on failure
- [x] 7.3 Refactor CD to consume the exact successfully validated build artifact, apply least-privilege job permissions and prevent promotion after any failed gate
- [x] 7.4 Pin or explicitly govern third-party workflow actions and test workflow syntax, dependencies and branch/tag promotion conditions

## 8. Final Evidence and Delivery Readiness

- [x] 8.1 Re-run all checks from a clean locked dependency installation and save a concise evidence report with versions, test counts, coverage, browser matrix, artifact sizes and audit result
- [x] 8.2 Reconcile and mark only evidenced tasks in both active OpenSpec changes, documenting any superseded implementation path without losing history
- [x] 8.3 Validate every active OpenSpec change and the full repository strictly with zero errors
- [x] 8.4 Run the complete project release gate once more after documentation and task synchronization and confirm 100 percent of required gates and critical journeys pass
